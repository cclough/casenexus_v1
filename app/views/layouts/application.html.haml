!!!
%html
  %head
    %title= full_title(yield(:title))
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    / IE shim
    = render 'layouts/shim'

  %body
    / header
    = render 'layouts/header'

    / flash div 
    #flash_application
      = render :partial => 'layouts/flash' , :locals => { :flash => flash }

    .container.main

      ////// to see request text
      / <haml:loud> debug(params) if Rails.env.development? </haml:loud>

      / :content now specified in all view - anoying, but enables nested views for 
      / e.g. for notifications, feedback controllers
      = yield :content

      / footer
      = render 'layouts/footer'

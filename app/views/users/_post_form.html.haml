= form_for(@post, remote: true) do |f|
  = render 'shared/error_messages', object: @post
  .field
    = f.text_area :content, placeholder: "Compose new post...", :rows => 6
  = f.submit "Post", class: "btn btn-large btn-primary"